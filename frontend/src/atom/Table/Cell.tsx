"use client"
import { books } from '@/features/Books/bookSlice'
import { TableCell, TableRow } from '@mui/material'
import React from 'react'

const Cell = (row : books) => {
    console.log(row)
  return (
        <TableCell>

        </TableCell>
  )
}

export default Cell
